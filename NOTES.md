# 원본 워크북에서 조정하고 더한 것

원본(`python-workbook-notion-md.zip`)은 **읽기 좋은 교재**입니다.
다만 "직접 풀어 보기"에는 빈틈이 있어 그 부분을 채웠습니다. 무엇을 왜 바꿨는지 남겨 둡니다.

## 1. 기초 트랙(B0~B7)에 채점이 아예 없었다 — 가장 큰 빈틈

원본에서 기초 트랙 실습은 "출력해 보세요" 형태라 맞았는지 확인할 방법이 없었습니다.
**프로그래밍이 처음인 사람일수록 피드백 루프가 절실한데**, 정작 그 트랙에만 채점이 없었던 셈입니다.

→ B1~B7 실습을 **값을 돌려주는 함수**로 바꿔 테스트를 붙였습니다.
   `print` 로 화면에 뿌리는 대신 `return` 하게 만든 건 그 자체가 B5의 학습 목표이기도 합니다
   (`test_returns_instead_of_printing` 이 print 로 짠 답을 잡아냅니다).

### 그런데 원본의 기초 트랙 자체가 얇았습니다 — 도구편 B8~B12 를 새로 썼습니다

원본 기초 트랙(B0~B7)은 코어로 넘어가기 전 **가벼운 진입로**로 만들어진 것이라
실습이 짧습니다. 그 실습만 테스트로 옮기니 얇음을 그대로 물려받았고, 그 결과
아래 것들이 **기초 어디에도 없었습니다**:

`while` · `break` · `continue` · `enumerate` · `zip` · `.items()` ·
문자열 메서드(`split` `join` `strip` `replace`) · 슬라이싱 ·
리스트/딕셔너리 컴프리헨션 · `range(a, b, step)` · `try`/`except` ·
`None` 다루기 · 파일 읽기·쓰기 · `if __name__ == "__main__"`

특히 B4의 `grade()` 가 `ValueError` 를 던지게 해 놓고 **그걸 잡는 법은 어디서도
가르치지 않는** 상태였습니다.

### 함수·클래스·타입도 얇았습니다 — 심화편 B13~B15

도구편을 붙인 뒤에도 구멍이 남아 있었습니다. B5 함수는 함수 5개, B6 클래스는 클래스 2개가
전부였고, 기초 트랙 전체에 이런 것들이 **하나도** 없었습니다:

- 함수: 키워드 전용 인자 · `*args`/`**kwargs` · 여러 값 반환 · `lambda` 실습 ·
  클로저 · `global`/`nonlocal` · 재귀 · 함수를 값으로 넘기기
- 클래스: **상속이 아예 없었습니다** · `super()` · 오버라이드 ·
  `__str__`/`__repr__`/`__eq__` · 클래스 변수 · `@property` ·
  `@classmethod`/`@staticmethod` · `isinstance`
- 타입: **타입 힌트를 가르치지도 시험하지도 않았습니다.** 모든 스켈레톤에 `-> str` 이
  붙어 있는데 설명이 한 줄도 없었고, 코어 15와 백엔드 트랙 전체가 그 위에 서 있습니다.

B13~B15(96문제)를 추가했습니다. B15는 힌트를 **직접 쓰게** 만들고
`typing.get_type_hints()` 로 실제로 붙었는지 검사합니다. 그리고 B1 맨 위에
"`: str` 이 뭔지 궁금하면 B15에서 배웁니다"라는 예고를 한 줄 넣었습니다 —
첫날 스켈레톤을 열자마자 만나는 문법이니까요.

도구편 5개 파트(113문제)는 다음과 같습니다. 원본에 없던 내용이라
각 파일 상단에 그 파트에서 쓰는 도구 목록을 정리해 두었고, 함정도 함께 적었습니다:

- `join` 은 구분자가 앞에 온다 (`", ".join(xs)`)
- `list.remove()` 는 **첫 번째 하나만** 지운다
- `xs.sort()` 는 원본을 바꾸고 `sorted(xs)` 는 새 리스트를 준다
- `f.write()` 는 `print` 와 달리 줄바꿈을 붙여 주지 않는다
- `open(..., "w")` 는 기존 내용을 **전부 지운다** (로그 날려 먹는 실수)
- `encoding="utf-8"` 을 안 적으면 윈도우에서 한글이 깨진다
- 벌거벗은 `except:` 는 Ctrl+C 까지 삼킨다
- `while` 에서 카운터 줄이는 걸 빠뜨리면 멈추지 않는다

## 2. 채점 키트(KIT)가 코어 20개 파트 중 일부만 다뤘다

원본 KIT은 20개 함수만 테스트합니다. 나머지 드릴 — LRU 캐시, match 라우터,
무한 제너레이터, 마스킹 감사 로그, ExceptionGroup, JSON 왕복, Semaphore 동시성 제한 등 — 은
"해 보세요"로만 남아 정답을 확인할 수 없었습니다.

→ `src/extra/` 에 기계적으로 판정 가능한 것들을 골라 125개 테스트를 추가했습니다.
   (측정·관찰형 문제 — `dis` 비교, `tracemalloc` 피크, 프로파일링 — 는 자동 채점이 어렵고
   직접 눈으로 보는 게 요점이라 그대로 두었습니다. 문서에 남아 있습니다.)

## 3. 문제가 애매했던 곳을 명확히 했다

| 원본 | 문제 | 조정 |
| --- | --- | --- |
| `retry(times=3)` "최대 times회까지 재시도" | 3번 재시도인지 3번 시도인지 모호 — 테스트는 총 시도로 판정 | docstring에 "총 시도 횟수"로 명시. `test_times_is_total_attempts_not_extra_retries` 추가 |
| `load_config` "파일이 없으면 ConfigError" | 테스트는 JSON 파싱까지 요구하는데 설명에 없음 | JSON 파일임을 명시. 깨진 JSON도 `ConfigError` 로 감싸도록 테스트 추가 |
| `BankAccount.withdraw` 가 잔액 부족 시 `"잔액 부족"` **문자열** 반환 | 호출한 쪽이 숫자인지 문자열인지 매번 확인해야 하고, 안 하면 조용히 망가짐 | `ValueError` 를 내도록 변경. 왜 바꿨는지 docstring에 설명 |
| `average(numbers)` | 빈 리스트에서 `ZeroDivisionError` — 왜 터졌는지 안 알려줌 | 빈 리스트는 `ValueError` 로 명시적으로 거부 |
| `grade(score)` | 범위 밖 점수를 조용히 통과 | 0~100 밖은 `ValueError`. "잘못된 입력을 조용히 넘기지 말 것"이 진짜 주제 |
| DRILL 11 "`+`, `-`, 비교" | KIT은 `+` 만 테스트 | `__sub__` 테스트 추가 |
| `Temperature` | `fahrenheit` 를 필드로 저장해도 원본 테스트는 통과 | `celsius` 변경이 `fahrenheit` 에 반영되는지 검사 추가 |
| `day_range_utc` DST | 23시간(서머타임 시작)만 검사 | 25시간(종료일)도 검사 — `start + 24시간` 으로 짠 답을 잡아냄 |

## 4. 더한 문제들

원본이 짚은 함정인데 연습 문제가 없던 것들입니다.

- **`display_width` / `pad_display`** — 원본 03파트가 "한글은 폭이 두 배라 `<12` 정렬이 어긋난다"고
  지적만 하고 넘어갑니다. 실제로 고쳐 보는 문제로 만들었습니다.
  (이 저장소의 채점 진행률 표시도 같은 함수로 정렬합니다 — `conftest.py` 를 열어 보세요)
- **`merge_deep`** — 설정 파일 병합. 실무에서 매번 쓰는데 원본에 없었습니다.
- **`unique_everseen`, `top_n`, `intersect`** — 게으름과 복잡도를 몸으로 느끼는 문제.
- **`describe_chain`** — DRILL 13의 2번(`from exc` / `from None` 차이)은 "직접 확인하세요"로만
  끝났습니다. 예외 연결 고리를 코드로 훑게 만들어 눈이 아니라 손으로 확인하게 했습니다.
- **`first_success`** — 여러 소스에 동시에 물어보고 나머지를 취소하는 패턴. 취소를 빠뜨리면
  테스트가 잡아냅니다.
- **캡스톤 A에 테스트 28개** — 원본은 요구사항 목록만 있었습니다. 파서·파이프라인·집계는
  기계적으로 검증 가능해서 붙였고, CLI와 병렬 처리는 직접 만들고 확인하도록 남겨 뒀습니다.

## 5. 구조를 바꾼 것

- 원본 KIT의 단일 `solutions.py`(20개 함수) → 주제별 4개 모듈로 분리.
  파일 하나가 길어지면 "지금 뭘 하고 있는지"를 놓칩니다.
- 채점 요약에 **파트별 진행률**과 **다음에 풀 문제 하나**를 표시.
  실패 목록 200줄을 보면 어디서 시작할지 모르게 됩니다.
- `STUDY_SOLUTIONS=1` 로 모범답안을 채점 대상으로 바꿀 수 있게 했습니다(`./study verify`).
  296개 문제가 전부 실제로 풀리는지 검증하는 데 씁니다.

## 6. 백엔드 트랙(B1~B4)에 채점을 붙였습니다

원본에서 백엔드 트랙은 **문서와 드릴 목록만** 있었습니다. "N+1을 만들어 보세요",
"경쟁 조건을 재현하세요" 같은 문제라 DB와 서버가 있어야 확인이 가능했고,
그래서 대부분은 읽고 넘어가게 됩니다.

DB 서버 없이 채점되도록 만들었습니다:

- **SQLAlchemy** — 인메모리 SQLite(aiosqlite)로 **진짜 쿼리를 날립니다.**
  `count_queries()` 로 실제 SQL 개수를 세기 때문에 N+1 은 "느리다"가 아니라
  "쿼리가 2개여야 하는데 4개다"로 잡힙니다.
- **`with_for_update()`** — SQLite 는 FOR UPDATE 를 지원하지 않아 조용히 무시합니다.
  그래서 문(statement)을 PostgreSQL 방언으로 컴파일해 `FOR UPDATE` 가 붙었는지 검사합니다.
  진짜 경쟁 조건 재현은 Postgres 로 직접 하도록 남겨 뒀습니다.
- **FastAPI** — httpx 의 `ASGITransport` 로 서버를 띄우지 않고 앱을 직접 호출합니다.
- **`async def` + 블로킹 함정(B2-4)** — 동시 요청 5개의 총 시간을 재서
  `async def` 안에서 `time.sleep` 을 부르면 실패하게 했습니다. 원문에서 "직접 재 보세요"로만
  끝나던 문제인데, 실무에서 조용히 성능을 죽이는 대표적인 실수라 테스트로 박았습니다.
- **리포지터리 Protocol(B3-4)** — 인메모리 구현과 SQLAlchemy 구현에 **같은 테스트를
  둘 다** 돌립니다. 인터페이스만 같은 게 아니라 동작이 같아야 교체 가능한 것이니까요.
- **graceful shutdown(B4-3)** — 시그널 처리는 테스트가 어려워, 정책 부분(`GracefulShutdown`)만
  클래스로 떼어 검증합니다. `signal.SIGTERM` 배선과 `docker stop` 확인은 원문대로 수동입니다.

채점 못 붙인 것: alembic 마이그레이션 생성(B3-1), `/docs` 눈으로 확인(B2-1),
Dockerfile 멀티스테이지 최적화(B4-4). 문서에 그대로 남아 있습니다.

## 7. 예측 퀴즈를 새로 만들었습니다

원본에도 예측 문제가 흩어져 있습니다 — *"다음 코드의 출력을 예측하고 이유를 쓰세요"*
(DRILL 01-1, 02-2, 04-1 …). 그런데 채점이 안 되니 대부분 그냥 넘어가게 됩니다.

`./study quiz` 로 37문제를 물어봅니다. 원본의 예측 드릴에, 문서 본문의
⚠️ Gotcha 로만 언급되고 연습 문제가 없던 것들을 더했습니다:

- 가변 기본 인자 (`def add(item, target=[])`)
- `[[0] * 2] * 3` — 같은 행 객체 3개
- 늦은 바인딩 (`[lambda: i for i in range(3)]`)
- `__eq__` 만 정의하면 `__hash__` 가 None 이 되는 것
- 순회 중 리스트 변경으로 원소가 건너뛰어지는 것
- `except ... as e` 의 e 가 블록 밖에서 사라지는 것
- `try/finally` 에서 finally 가 return 값보다 먼저 찍히는 것

**JS/TS 를 쓰다 파이썬에 오면 틀리는 것들**도 따로 넣었습니다:
`7 % -2` 의 부호, `bool([])` 와 `bool("0")`, `all([])` 가 True 인 것, `zip` 의 조용한 잘림.

### 정답을 실행으로 검증합니다

**틀린 정답이 든 퀴즈는 안 배우느니만 못합니다.** 그래서 모든 문제의 `code` 를
실제로 실행해 `answer` 와 대조하는 테스트를 붙였습니다(`quiz/tests/`).

실제로 이 검증이 처음 돌렸을 때 **제가 적어 둔 정답 2개가 틀린 걸 잡아냈습니다**:

| 문제 | 적어 둔 답 | 실제 |
| --- | --- | --- |
| 가변 기본 인자 `print(add(1), add(2))` | `[1] [1, 2]` | `[1, 2] [1, 2]` — 둘 다 **같은 객체**라 print 시점엔 이미 둘 다 [1,2] |
| 순회 중 remove `[1,2,3,4]` | `[1, 3, 4]` | `[1, 3]` — 3 이 건너뛰어진 뒤 4 도 지워짐 |

정답 비교는 공백·따옴표·대소문자를 무시합니다. `[1, 2, 4]` 와 `[1,2,4]` 를 같게 보고,
`'a'` 와 `"a"` 를 같게 봅니다 — 개념을 맞혔는지가 중요하지 띄어쓰기가 중요한 게 아니니까요.

## 8. 손대지 않은 것

- 문서 43개(`docs/`)는 원본 그대로입니다.
- 측정·관찰형 드릴(`dis`, `timeit`, `tracemalloc`, `cProfile`, `gc`)은 자동 채점을 붙이지
  않았습니다. 숫자를 직접 보는 것이 목적인 문제라 테스트로 바꾸면 요점이 사라집니다.
- 데이터 트랙(D1~D4)도 마찬가지입니다. 라이브러리 의존성만 `pyproject.toml` 의
  선택 그룹으로 준비해 두었습니다.
- 내부 트랙(22~24)은 원문대로 "성능 문제를 실제로 만났을 때 펼치는" 참고 자료로 두었습니다.
